/*
REMOVE ELEMENT
SAVE TO TEXT FILE
BUTTONS



ENTER FUNCTION - SEND TO TEXT FILE
CHANGE COLOR
CHANGE FONT

SAVE BOX TO FILE AND LOAD BOXES ON STARTUP
String playerDataFile = "player.dat";             //defines the (lack of) path and filename

if (loadStrings(statisticsFile) == null) {        //checks if the file is there
saveStrings(playerDataFile, playerDataArray);     //saves some empty data if it isn't
}

Then loading works fine too in the usual way:

playerDataArray =  loadStrings(playerDataFile);    //bob's your uncle







*/
